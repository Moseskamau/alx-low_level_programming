#include "main.h"

/**
 * mul - Multiplies two integers
 * @a: First num to multiply
 * @b: Second num to multiply with first num
 * Return: Result of multiplication of parameter and b parameter
 */

int mul(int a, int b)
{
	return (a * b);
}
